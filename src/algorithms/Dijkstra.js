export default class Dijkstra {
  constructor() {
    console.log("Dijkstra constructor");
  }

  shortestPath(graph, source) {
    //
    /*
    //assign all nodes a distance of infinite/-1 except for source
    //start at the source
    //
    */
  }

  findMinDist(dist, visited) {
    //
  }
}
